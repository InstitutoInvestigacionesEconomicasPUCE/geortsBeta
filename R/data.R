#' Original Time Series
#' @format A time series object with \code{ncol()=3}
"TS0"

#' Positions of original time series
#' @format A data frame with two columns named \code{lon} (longitud), and \code{lat} (latitude), asociated to the time series
"positions.TS0"

#' Weights of original time series
#' @format A vector of length equal to number of rows of \code{positions.TS0}, asociate with the influence of original time series
"weights.TS0"

#' Positions of nwe time series
#' @format A data frame with two columns named \code{lon} (longitud), and \code{lat} (latitude)
"positions.RTS0"
